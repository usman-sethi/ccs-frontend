import AuthPage from "@/features/auth/AuthPage";

export const metadata = {
  title: "Sign in — CCS",
  description: "Sign in or create your Core Computing Society member account.",
};

export default function Page() {
  return <AuthPage />;
}
