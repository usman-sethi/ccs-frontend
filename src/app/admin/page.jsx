import AdminCustomizePage from "@/features/admin/AdminCustomizePage";

export const metadata = {
  title: "Admin — Customize | CCS",
  description: "CCS admin panel — customize all site content.",
};

export default function Page() {
  return <AdminCustomizePage />;
}
