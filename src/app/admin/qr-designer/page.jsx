import QrDesignerPage from "@/features/admin/QrDesignerPage";

export const metadata = {
  title: "QR Designer | Admin — CCS",
  description: "Create beautiful styled QR codes for CCS events.",
};

export default function Page() {
  return <QrDesignerPage />;
}
